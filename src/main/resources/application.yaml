spring:
  application:
    name: RecipeService
  datasource:
    url: jdbc:postgresql://localhost:5432/recipeservice
    username: admin
    password: secret
  grpc:
    server:
      servlet:
        enabled: false